"git init" is used one time in a project

1. "git add ." is used everytime to add 
2. git commit  -m "first commit" is used everytime to add 
3. "git push" is used everytime to add 


git init
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/bsbahauddin822/my-first-github.git
git push -u origin main